These are iteria's ten narrative proposals. Once the container is running, open the app,
click "Add past proposals", upload these files, and set the outcome (Brown County = lost,
the rest = in progress). Ingestion classifies, chunks, tags, and embeds them into Oracle.
The library is then live for drafting and the assistant.
